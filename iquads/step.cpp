#include <vector>
#include <string>
#include <boost/algorithm/string.hpp>

namespace iquads{

void step :: generate_input( const string input )
{

  if( boost :: iequals( keyword, "dmrg" ) ){
   
  }
  if( boost :: iequals( keyword, "mps_response") ){

  }
  if( boost :: iequals( keyword, "int_transform") ){

  }
  if( boost :: iequals( keyword, "mps_overlap") ){

  }
  if( boost :: iequals( keyword, "hf_orb_response" ) ){

  }
  if( boost :: iequals( keyword, "dmrg_ci_energy_gradient" ) ){

  }
  if( boost :: iequals( keyword, "dmrg_scf_energy_gradient" ) ){

  }

}

}
